{-
Lets play with probabilistic context free grammaers!
-}
module Pcfg where

-- Alright, so all grammars are in fact a subset of pure functional programming.
-- lets keep to that.

start :: 

data NonTerminals = Start | Stop

model :: NonTerminals -> () 